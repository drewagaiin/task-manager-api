# Copia este archivo como ".env" (sin el .example) y pon tu propia clave.
# El .env real NUNCA se sube a GitHub (está en .gitignore) — este archivo
# .env.example sí se sube, solo para que cualquiera sepa qué variables
# necesita configurar para correr el proyecto.
API_KEY=cambia-esto-por-tu-propia-clave
